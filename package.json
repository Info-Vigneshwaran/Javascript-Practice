{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "prompt": "^1.2.1",
    "save": "^2.4.0"
  }
}
